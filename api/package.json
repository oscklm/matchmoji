{
  "name": "matchmoji-api",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute matchmoji --local --file=schema.sql",
    "db:remote": "wrangler d1 execute matchmoji --remote --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4"
  }
}
